services:

  fastapi:
    build:
      context: ./fastapi
      dockerfile: Dockerfile
    container_name: fastapi-edmuhlestein
    restart: unless-stopped
    environment:
      - DATABASE_URL=${DATABASE_URL}
    expose:
      - "8000"                         # Internal port (no external exposure)
    networks:
      - ednet

networks:
  ednet:
    driver: bridge
